'use strict';

function sum(a, b) {
  return a + b;
}

sum(5, 10);

const sum2 = (a, b) => a + b;
sum2(10, 20);

function sub(a, b) {
  return a - b;
}

sub(5, 10);

const mult = (a, b) => a * b;
mult(3, 8);